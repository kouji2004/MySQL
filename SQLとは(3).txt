SQLとは　

構造化した、問い合わせる

データを入れる作業もSQLで行える

データの入れ方
INSERT INTO テーブル名（users）SET カラム名="〇〇"(例.name="ミウラ")　

実際にコードで書いてみる
INSERT INTO users SET name="ミウラ",age=27,gender="男";

※数字にはダブルクォーテーションを付けない、処理の最後にコロン(;)をつける。

ルール　SQLは原則大文字
 　　　テーブルやカラム名は小文字